{
  "entities": {
    "PortfolioContent": {
      "title": "PortfolioContent",
      "description": "Entire portfolio data including projects, about, hero, and contact info",
      "type": "object",
      "properties": {
        "content": {
          "type": "object",
          "description": "Full site content JSON payload"
        },
        "updatedAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp of the last update"
        }
      },
      "required": ["content", "updatedAt"]
    },
    "PortfolioProject": {
      "title": "PortfolioProject",
      "description": "Individual project document with detailed sections and media",
      "type": "object",
      "properties": {
        "id": {
          "type": "string"
        },
        "title": {
          "type": "string"
        },
        "updatedAt": {
          "type": "string",
          "format": "date-time"
        }
      },
      "required": ["id", "title"]
    }
  },
  "firestore": {
    "/portfolio_content/{docId}": {
      "schema": "PortfolioContent",
      "description": "Global portfolio configuration document shared across all visitors"
    },
    "/portfolio_projects/{projectId}": {
      "schema": "PortfolioProject",
      "description": "Individual project records for modular high-capacity storage"
    }
  }
}
